## Summary


## Checklist
- [ ] Tests written for all new features and edge cases
- [ ] Documentation updated if this changes user-facing behaviour, CLI flags, or architecture
- [ ] `make test && make lint` passes locally
- [ ] Error messages are plain English (no raw OS errors exposed to the user)
- [ ] New listing commands have a `--json` flag; default output is human-readable
