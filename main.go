package main

import "fmt"

func main () {

}

// tmux basic units
// session, window, pane

// vault basic units
// vault dirs: project, area, meta
// code dirs: code/<project>/<repo>

// basic logic:
// session list is populated by each project and area (n each) and a single meta session and a single vault session (vault root) 
// windows are populated by each repo in the code dir corresponding to a project 

// ui:
// session picker, window picker

